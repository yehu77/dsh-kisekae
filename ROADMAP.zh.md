# DSH Kisekae 路线图

Status: in progress

[English](ROADMAP.md) | 中文

当前里程碑：阶段 2——在浏览器内持久选择“官方外观”和“DeepSeek蓝鲸娘”，同时开始阶段 6 的桌面外壳基础。在另外两套原创皮肤证明目录确实由数据驱动前，阶段 2 仍未完成。

首版图鉴、随阶段变化的主对话背景、“蓝鲸玻璃侧栏·雨幕”和浏览器本地文字风格选择器已经实现：42 张发布图片全部进入图鉴；“固定／随机／关闭”控制 `conversation.backdrop`，“清爽／沉浸／关闭”则控制使用固定图片的 `sidebar.backdrop`。输入框通过 `conversation.composer.bar.decoration` 增加无图片海玻璃；“新会话”按钮增加 `sidebar.newSession.decoration` 和海浪对话 `sidebar.newSession.icon`；“设置”入口则增加 `settings.trigger.decoration`。这六项视觉贡献跟随当前预览的皮肤，而已存美术偏好保持独立。文字风格可以跟随主题默认、恢复官方清晰正文，或保留主题配色并关闭文字特效。

## 目标

在不拆分 Web 与桌面体验的前提下，为 DeepSeek Harness 建立一套可维护、开源的二次元风格皮肤合集。安装、选择、预览和移除都必须可逆；更丰富的美术表现绝不能变成使用智能体的必要条件。

## 边界

- 官方浅色、深色和跟随系统偏好保持不变。
- Kisekae 皮肤是一个 `theme.overrideTokens()` 层，每个覆盖值都同时提供浅色和深色版本。
- 首个版本只使用原创或获得适当授权的美术，不纳入可识别的作品角色素材。
- 首个桌面基线只是同一个本地 Web profile 的安全外壳，暂不包含打包运行时、更新器、远程市场、任意用户 CSS、浏览器下载代码、模型人格或另一套会话布局。
- 设置选择器、装饰 overlay 和 onboarding 即使共享美术方向，也保持为不同功能。

## 交付阶段

### 0. 基础与基线

- 保持一个仓库中的两个可独立安装包：自包含主题包携带 `dsh.bundle`、Host 入口和 `dsh.client`，主题中立的对话底座则继续使用官方包身份。
- 固定受支持的 DeepSeek Harness commit，并记录未修改 Web UI 的基线。
- 盘点设置、会话、Markdown、代码、工具、终端、overlay 和焦点状态的语义令牌。
- 批准一个响应式选择器线框图、一套原创视觉方向和素材政策。

退出条件是：链接安装的包可以加载和释放，并且官方外观保持不变。

### 1. 一个可逆皮肤

- 实现“DeepSeek蓝鲸娘”原创纯令牌皮肤，并完整提供浅色与深色值。
- 校验定义 id、令牌值、替换、模式切换和释放行为。
- 不使用私有选择器，也不请求上游变更。

退出条件是：浅色、深色和跟随系统都正常工作，热释放恢复官方外观，而且主要 Web 表面中的内容保持可读。

### 2. 主题包 MVP

- 使用按当前 origin 隔离的版本化浏览器存储保存已应用皮肤，并通过 `storage` 事件同步同源标签页。
- 让独立 Client bundler 与当前 Harness 平台 externals、依赖纯度检查和环境替换保持一致；组件局部样式确实需要 CSS Modules 时，再先补齐由 effect 管理的样式注入。
- 添加带即时本地预览、“取消／应用／官方外观”的响应式设置选择器。
- 添加即时持久化的文字风格选择器，提供“主题默认／官方清晰／关闭文字特效”。
- 再增加两个原创皮肤，证明主题由数据定义。
- 已存皮肤不可用时，明确回退到官方外观。

退出条件是：选择在同一浏览器和 origin 中经受刷新与服务器重启，同源标签页在应用后保持一致，浏览器与 origin 的隔离范围有明确文档，仅键盘操作可用，移除包后留下普通 Web profile。

### 3. 可选的丰富表现与指引

- 通过“固定／随机／关闭”让已经发布的主 `conversation.backdrop` 始终保持可选。原生横向壁纸使用完全不透明的 `cover` 铺满沉浸画布；普通插画继续以完全不透明的 `contain` 前景展示整张未裁切图片，再用未模糊的低透明度 `cover` 副本填补剩余画布。文字保护由独立的内容表层负责，不再用整幅淡化处理背景。
- 通过“清爽／沉浸／关闭”让已经发布的玻璃侧栏保持可选。宽侧栏使用一张固定的图鉴图片并向上渐隐，窄栏只使用安静的语义渐变。
- 通过宿主提供的宽栏状态保持“新会话”皮肤响应式：宽栏结合右侧低透明图片与语义玻璃，窄栏使用紧凑玻璃。海浪对话图标只改变图形；两项贡献都不替换官方按钮或操作。
- 保持“设置”入口皮肤响应式，同时不替换官方齿轮、文字、按钮或对话框行为。宽栏使用右侧低透明图片与保证文字可读的语义遮罩，窄栏使用无图片玻璃和轻微涟漪。
- 保持已发布的输入框皮肤位于官方 textarea 和控件背后。Hero 使用随模式变化的海洋玻璃、双层发光边、三道潮汐线、左右鲸尾、按钮光圈、珍珠、海星和贝壳，常驻对话输入框降低装饰强度；它只使用可缩放 CSS 和内联 SVG，不使用位图或模糊，也不替换焦点、文件拖放或高度变化行为。
- 保持字体与文字效果由主题拥有。“主题默认”解析为当前皮肤的字体和效果；“官方清晰”和“关闭文字特效”作为通用用户覆盖，与所选皮肤分别保存。
- 仅在蓝鲸娘是当前可见 draft 时挂载主对话背景、输入框装饰、侧栏背景、“新会话”装饰、海浪对话图标和“设置”入口装饰。预览官方外观会移除整组；点击“取消”会恢复已存皮肤对应的整组，同时不清除偏好。
- 保持主对话背景点击穿透，并保护消息与输入区可读性；短暂透明度过渡必须在减少动态效果偏好下关闭。
- 把任何首次运行指引实现成独立的 `settings.onboarding` 贡献。

退出条件是：装饰和指引都可以在不改变所选皮肤的情况下独立禁用，也不会阻塞会话、工具、凭据或设置。

### 4. 公开分发

- 从维护者控制的 npm scope 发布，并保留固定 commit 的 GitHub 安装方式。
- 记录安装、升级、移除、兼容性、素材署名、创作规则和故障排查。
- 为可见发布流程提供真实服务器截图和浏览器交互录制。

退出条件是：干净的 Web profile 只使用文档命令即可安装、运行、升级和移除 Kisekae。

### 5. 社区创作工具包

- 第二个由独立维护者负责的主题包出现后，再提取公开定义库。
- 增加 schema 校验器、预览 fixture、令牌覆盖报告和作者模板。
- 发布贡献、兼容性、授权与包信任指引。

退出条件是：新主题包无需导入 Kisekae 运行时内部实现即可构建；托管市场仍作为独立提案处理。

### 6. 桌面端分发

- 保持 `apps/desktop` 是两个可独立安装插件旁边的应用；不得把桌面打包变成第三个主题运行时。
- 开发期间接入已经在稳定地址 `127.0.0.1:3080` 运行的完整 Harness，或者在该地址启动相邻源码仓库；不得静默创建第二个端口或第二个 profile 实例。
- 为没有 Node、pnpm 或源码仓库的普通用户打包经过审核的 Node 24 运行时、受支持的 Harness 发行内容和两个 Kisekae 包。
- 保持 renderer 沙箱化并关闭 Node integration；原生桌面功能只能通过狭窄的主进程 capability 加入。
- 在桌面端进入通用可用状态前，提供签名与公证后的 macOS 构建、签名后的 Windows 安装包、异常退出后的子进程清理和明确的更新通道。

退出条件是：一台干净的受支持设备可以通过普通图形流程安装、启动、更新和移除桌面应用，同时 Web-only 安装方式继续可用。

## 决策门

- 如果令牌无法表达可信的第一套皮肤，列出缺失语义角色，并且只提议由两个消费方证明的通用 alias；不得用私有 CSS 绕过。
- 如果皮肤激活产生可测量的首屏闪烁，提议通用的已安装令牌 bootstrap hook；不得修改生成后的 HTML。
- 发布图鉴素材由 Kisekae 的同源 Host 路由从包内浏览器副本提供；不得依赖 Client 构建相邻文件、inbox 原图或远程 CDN。
- 产品中立的 `conversation.backdrop`、`conversation.composer.bar.decoration`、`sidebar.backdrop`、`sidebar.newSession.decoration`、`sidebar.newSession.icon` 和 `settings.trigger.decoration` 分别承载主画布美术、输入框材质、侧栏美术与主题化按钮皮肤。其他视觉表面仍应先提供自己的通用 seam，不得使用私有选择器。
- 聊天背景、输入框和侧边栏样式分别保持可选。复杂图片不得直接垫在消息或可编辑文字下方；每个表面都要保留安静、清晰的可读方案。
- 所有美术、颜色、字体和文字效果定义都留在主题包中。对话底座可以提供通用呈现接口，但不得包含 Kisekae 配色或字体选择。
- 在 Harness 仅暴露 allowlist 内设置 namespace 期间，选择由 Kisekae 自己的版本化浏览器存储负责。不得为 Kisekae 修改上游 allowlist；只有出现通用的仓库外扩展点后，才重新评估 Host 持久化。
- 任何上游 API 变更都需要自己的 DeepSeek Harness 设计记录、当前状态文档、可逆 effect 和组装 Web 测试。

## 发布标准

- 安装和移除不需要编辑 DeepSeek Harness checkout。
- 恰好只有一个已确认皮肤层处于活动状态，预览和释放都会恢复之前的令牌状态。
- 主题使用语义令牌和本地打包的授权素材，不远程加载，也不运行分析。
- 每个皮肤都支持键盘操作、清晰焦点、内容可读、减少动态效果、缩放和窄屏布局。
- 测试覆盖定义校验、持久化、回退、替换与释放，并由真实 Web profile 证明组装行为。
- 代码与素材许可证保持分离，每个发布的非代码文件都有来源元数据。
- 桌面外壳最多拥有一个 Harness 进程，保持一个稳定的本地 origin，并且不向 Web 内容公开不受限制的 Node 或 Electron API。
