# DSH Kisekae 路线图

Status: in progress

[English](ROADMAP.md) | 中文

当前里程碑：阶段 2——在浏览器内持久选择“官方外观”和“DeepSeek蓝鲸娘”。在另外两套原创皮肤证明目录确实由数据驱动前，阶段 2 仍未完成。

首版图鉴、可选的右下角装饰和“蓝鲸玻璃侧栏·雨幕”已经实现：42 张发布图片全部进入图鉴；“固定／随机／关闭”控制一个带画框的 `shell.overlay`，“清爽／沉浸／关闭”则控制使用固定图片的 `sidebar.backdrop`，海浪对话图标还会占用 `sidebar.newSession.icon`。这些贡献都不影响当前选择的皮肤。

## 目标

在不 fork 官方 UI 的前提下，为 DeepSeek Harness 建立一套可维护、开源的二次元风格 Web 皮肤合集。安装、选择、预览和移除都必须可逆；更丰富的美术表现绝不能变成使用智能体的必要条件。

## 边界

- 官方浅色、深色和跟随系统偏好保持不变。
- Kisekae 皮肤是一个 `theme.overrideTokens()` 层，每个覆盖值都同时提供浅色和深色版本。
- 首个版本只使用原创或获得适当授权的美术，不纳入可识别的作品角色素材。
- 首个版本不包含远程市场、任意用户 CSS、浏览器下载代码、模型人格、桌面端表面或替换会话布局。
- 设置选择器、装饰 overlay 和 onboarding 即使共享美术方向，也保持为不同功能。

## 交付阶段

### 0. 基础与基线

- 保持一个自包含 npm 包，同时携带 `dsh.bundle`、Host 入口和 `dsh.client`。
- 固定受支持的 DeepSeek Harness commit，并记录未修改 Web UI 的基线。
- 盘点设置、会话、Markdown、代码、工具、终端、overlay 和焦点状态的语义令牌。
- 批准一个响应式选择器线框图、一套原创视觉方向和素材政策。

退出条件是：链接安装的包可以加载和释放，并且官方外观保持不变。

### 1. 一个可逆皮肤

- 实现“DeepSeek蓝鲸娘”原创纯令牌皮肤，并完整提供浅色与深色值。
- 校验定义 id、令牌值、替换、模式切换和释放行为。
- 不使用私有选择器，也不请求上游变更。

退出条件是：浅色、深色和跟随系统都正常工作，热释放恢复官方外观，而且主要 Web 表面中的内容保持可读。

### 2. 主题包 MVP

- 使用按当前 origin 隔离的版本化浏览器存储保存已应用皮肤，并通过 `storage` 事件同步同源标签页。
- 让独立 Client bundler 与当前 Harness 平台 externals、依赖纯度检查和环境替换保持一致；组件局部样式确实需要 CSS Modules 时，再先补齐由 effect 管理的样式注入。
- 添加带即时本地预览、“取消／应用／官方外观”的响应式设置选择器。
- 再增加两个原创皮肤，证明主题由数据定义。
- 已存皮肤不可用时，明确回退到官方外观。

退出条件是：选择在同一浏览器和 origin 中经受刷新与服务器重启，同源标签页在应用后保持一致，浏览器与 origin 的隔离范围有明确文档，仅键盘操作可用，移除包后留下普通 Web profile。

### 3. 可选的丰富表现与指引

- 通过“固定／随机／关闭”让已经发布的 `shell.overlay` 画框装饰始终保持可选。
- 通过“清爽／沉浸／关闭”让已经发布的玻璃侧栏保持可选。宽侧栏使用一张固定的图鉴图片并向上渐隐，窄栏只使用安静的语义渐变。
- 通过宿主提供的宽栏状态保持海浪对话“新会话”图标响应式，同时不替换官方按钮或操作。
- 保持静态装饰点击穿透并远离任务内容；未来增加任何动画时都必须遵循减少动态效果偏好。
- 后续把聊天背景和输入框样式作为相互独立的可选表面继续扩展。侧栏样式已经使用公开背景 slot 与语义行令牌；根据用途生成专门的浏览器副本来复用图鉴素材，不强行把每张图片塞进每个界面。
- 把任何首次运行指引实现成独立的 `settings.onboarding` 贡献。

退出条件是：装饰和指引都可以在不改变所选皮肤的情况下独立禁用，也不会阻塞会话、工具、凭据或设置。

### 4. 公开分发

- 从维护者控制的 npm scope 发布，并保留固定 commit 的 GitHub 安装方式。
- 记录安装、升级、移除、兼容性、素材署名、创作规则和故障排查。
- 为可见发布流程提供真实服务器截图和浏览器交互录制。

退出条件是：干净的 Web profile 只使用文档命令即可安装、运行、升级和移除 Kisekae。

### 5. 社区创作工具包

- 第二个由独立维护者负责的主题包出现后，再提取公开定义库。
- 增加 schema 校验器、预览 fixture、令牌覆盖报告和作者模板。
- 发布贡献、兼容性、授权与包信任指引。

退出条件是：新主题包无需导入 Kisekae 运行时内部实现即可构建；托管市场仍作为独立提案处理。

## 决策门

- 如果令牌无法表达可信的第一套皮肤，列出缺失语义角色，并且只提议由两个消费方证明的通用 alias；不得用私有 CSS 绕过。
- 如果皮肤激活产生可测量的首屏闪烁，提议通用的已安装令牌 bootstrap hook；不得修改生成后的 HTML。
- 发布图鉴素材由 Kisekae 的同源 Host 路由从包内浏览器副本提供；不得依赖 Client 构建相邻文件、inbox 原图或远程 CDN。
- 官方 `shell.overlay` 已足以承载右下角画框装饰，而产品中立的 `sidebar.backdrop` 和 `sidebar.newSession.icon` 分别承载侧栏美术与主题化“新会话”图形。其他视觉表面仍应先提供自己的通用 seam，不得使用私有选择器。
- 聊天背景、输入框和侧边栏样式分别保持可选。复杂图片不得直接垫在消息或可编辑文字下方；每个表面都要保留安静、清晰的可读方案。
- 在独立发布或所有权让拆包真正有用之前，主题定义保持内部实现。
- 在 Harness 仅暴露 allowlist 内设置 namespace 期间，选择由 Kisekae 自己的版本化浏览器存储负责。不得为 Kisekae 修改上游 allowlist；只有出现通用的仓库外扩展点后，才重新评估 Host 持久化。
- 任何上游 API 变更都需要自己的 DeepSeek Harness 设计记录、当前状态文档、可逆 effect 和组装 Web 测试。

## 发布标准

- 安装和移除不需要编辑 DeepSeek Harness checkout。
- 恰好只有一个已确认皮肤层处于活动状态，预览和释放都会恢复之前的令牌状态。
- 主题使用语义令牌和本地打包的授权素材，不远程加载，也不运行分析。
- 每个皮肤都支持键盘操作、清晰焦点、内容可读、减少动态效果、缩放和窄屏布局。
- 测试覆盖定义校验、持久化、回退、替换与释放，并由真实 Web profile 证明组装行为。
- 代码与素材许可证保持分离，每个发布的非代码文件都有来源元数据。
