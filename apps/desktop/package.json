{
  "name": "@yehu77/dsh-kisekae-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for DeepSeek Harness with DSH Kisekae.",
  "type": "module",
  "main": "./lib/main.js",
  "scripts": {
    "clean": "rimraf lib",
    "typecheck": "tsc --noEmit",
    "build": "pnpm run clean && tsdown",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "check": "pnpm run typecheck && pnpm run test",
    "start": "pnpm run build && electron ."
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "43.2.0",
    "rimraf": "^6.1.2",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  },
  "license": "MIT"
}
